# MAG/StbEmu User-Agent — used to internally serve /c/ at / (no 307).
# Include from http {} (all files in conf.d are already in http).
map $http_user_agent $nexlify_mag_ua {
    default 0;
    "~*(STBEmu|stbapp|MAG[0-9]{3}|Infomir)" 1;
}
