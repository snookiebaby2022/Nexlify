# Marketing website .env — copy to /var/www/nexlify/.env on VPS
# Or run: bash scripts/setup-marketing-env.sh (auto-copies secrets from panel .env)

# === Required — MUST be separate from panel DB (use setup-marketing-database.sh) ===
# NEVER point this at the panel database (nexlify) — db push will destroy panel tables.
DATABASE_URL="postgresql://nexlify:PASSWORD@127.0.0.1:5432/nexlify_marketing"
JWT_SECRET="long-random-string-min-32-chars"

# License signing — use FILE path only (never paste PEM lines into .env)
LICENSE_KEY_FILE=/var/www/nexlify/.license-keys/private.pem
NEXLIFY_LICENSE_API_URL=http://127.0.0.1:8787

# === Site URLs ===
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://nexlify.live
NEXT_PUBLIC_WEBSITE_URL=https://nexlify.live
NEXT_PUBLIC_PANEL_URL=https://panel.nexlify.live

# === Payments (required after Sep 1, 2026 promo) ===
# Create account: https://dashboard.stripe.com/register
# API keys: https://dashboard.stripe.com/apikeys (Live mode for production)
# Only the secret key is required — no webhook endpoint needed for current checkout flow.
STRIPE_SECRET_KEY=sk_live_xxxx

# === Panel API sync ===
NEXLIFY_PANEL_API_SECRET=your-shared-secret
BILLING_WEBHOOK_SECRET=your-billing-secret

# === Admin login (https://nexlify.live/admin) ===
ADMIN_EMAIL=admin@nexlify.live
ADMIN_PASSWORD=your-secure-password-min-8-chars

# === Optional: WHMCS ===
# WHMCS_API_SECRET=long-random-secret-min-32-chars
# NEXT_PUBLIC_WHMCS_URL=https://billing.yourdomain.com/cart.php

# === Optional: ad & analytics IDs (load via GTM + AdPixels) ===

# Google Search Console HTML tag verification (meta tag)
# GOOGLE_SITE_VERIFICATION=your-google-verification-code

# Meta (Facebook) Pixel — Ads Manager → Events Manager
# NEXT_PUBLIC_META_PIXEL_ID=

# TikTok Pixel — TikTok Ads → Events
# NEXT_PUBLIC_TIKTOK_PIXEL_ID=

# LinkedIn Insight Tag partner ID
# NEXT_PUBLIC_LINKEDIN_PARTNER_ID=

# Microsoft Clarity project ID — clarity.microsoft.com
# NEXT_PUBLIC_CLARITY_ID=

# Password reset + activation emails (required for /forgot-password and license activation codes)
# Quick setup on VPS: bash scripts/configure-marketing-smtp-stripe.sh
# Gmail: enable 2FA → Google Account → Security → App passwords → generate for "Mail"
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@gmail.com
# SMTP_PASS=your-16-char-app-password
# SMTP_FROM=Nexlify <noreply@nexlify.live>

# Newsletter webhook (Brevo/Mailchimp/Zapier) — optional; else logs to data/newsletter-subscribers.log
# NEWSLETTER_WEBHOOK_URL=https://hooks.example.com/newsletter

# Live stream (https://nexlify.live/livestream) — OBS RTMP ingest + HLS playback
# NEXT_PUBLIC_LIVESTREAM_HLS_URL=https://nexlify.live/hls/nexlify.m3u8
# LIVESTREAM_RTMP_SERVER=rtmp://nexlify.live/live
# LIVESTREAM_STREAM_KEY=your-secret-stream-key
# LIVESTREAM_TITLE=Nexlify Live
# LIVESTREAM_DESCRIPTION=Watch the Nexlify live broadcast.
