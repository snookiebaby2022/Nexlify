Optional marketing PNGs (homepage now uses live UI mocks by default).

If you add real screenshots, they can be used elsewhere:
- dashboard.png — operator dashboard
- billing.png — Stripe billing sync
- reseller.png — reseller hierarchy

Served at /screenshots/<name>.png
