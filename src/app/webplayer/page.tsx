import { PanelWebPlayer } from "@/components/panel-web-player";

export const metadata = {
  title: "Web Player — Nexlify",
  description: "Built-in browser IPTV player",
};

export default function WebPlayerPage() {
  return <PanelWebPlayer />;
}
