# /etc/nginx/conf.d/nexlify-upstream.conf
# Shared upstreams — do not redefine in site vhosts.

upstream nexlify_panel {
    server 127.0.0.1:3000;
    keepalive 8;
}

upstream nexlify_website {
    server 127.0.0.1:3001;
    keepalive 8;
}
