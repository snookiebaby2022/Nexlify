# Copy relevant keys into .env.test or export before npm run test:load

LOAD_BASE_URL=http://127.0.0.1:13100
# NEXLIFY_LOAD_ALLOW_REMOTE=1   # only for a dedicated staging host (never prod 45)

# Seed size (default 10000 — use 1000 for smoke)
# LOAD_CHANNEL_COUNT=10000
# LOAD_RESELLER_CREDITS=100

# Scale VUs down on laptops
# LOAD_AUTH_VUS=1000
# LOAD_LIVE_AUTH_VUS=5000
# LOAD_M3U_VUS=5
# LOAD_CREDIT_VUS=200
# LOAD_CREDIT_RACE_N=200

# Must match panel env for live-auth
# PANEL_INTERNAL_SECRET=e2e-panel-internal-secret
